# 不寫 code，也能做出自己的 iPhone app

這一頁給**不讀程式碼**的人。你用白話說想做什麼，AI 負責寫程式、測試和檢查；每做完一步，它會給你一張「試用卡」，請你在 iPhone 上試幾個步驟，試完回「OK」就好。

## 你需要

- 一台 Mac，從 App Store 裝好 **Xcode**，打開一次、同意授權條款
- **Claude 或 Codex**（擇一，兩個都有也行）：
  - Claude 桌面 app（推薦，可以看著 AI 操作模擬器）或 Claude Code
  - Codex 桌面 app（推薦）或 Codex CLI
- 一支 iPhone（沒有也可以，改在電腦上的模擬器試）
- 一個 Apple 帳號（免費的就可以）

## 安裝：把這段話貼給 Claude 或 Codex

```text
請幫我安裝 iOS vibe 開發工具：
1. 把 https://github.com/peter6601/ios-dev-skill 下載到 ~/Developer/ios-dev-skill（已經有就更新到最新）。
2. 在那個資料夾執行 ./install.sh --vibe --dry-run，用白話告訴我它會做哪些事。
3. 我同意之後，再執行 ./install.sh --vibe --yes，把結果用白話告訴我；有沒裝成功的，帶我一步一步補裝。
```

裝好後重新打開 Claude 或 Codex。**用 Codex 的話多一步**：輸入 `/hooks`，把「careful-ios」那一條設為信任。它會在 AI 想執行危險指令（例如刪掉整個資料夾）時擋下來，只要設一次。

## 開始

Claude 輸入 `/ios-vibe`、Codex 輸入 `$ios-vibe`，接著用白話說你想做什麼：

```text
/ios-vibe 我想做一個記帳 app，可以記每天花了多少錢
```

之後每次想加功能、改畫面、回報哪裡怪怪的，一樣用 `/ios-vibe`（Codex 是 `$ios-vibe`）開頭說就好。

## 過程中會發生什麼

1. **AI 先問你幾個問題**，都是關於 app 本身：給誰用、最常做的事、第一個畫面長什麼樣、資料要不要留著。一次最多幾題，每題都附它的建議。
2. **它自己決定所有技術細節**，你不會被問到看不懂的問題。它替你決定了什麼，都記在專案裡的 `docs/vibe-decisions.md`，之後找工程師幫忙時可以給對方看。
3. **它寫程式、跑測試、請另一個 AI 分身檢查**，全部過了才來找你。
4. **你拿到一張試用卡**，大概長這樣：

   > **試用卡：記帳 app 可以新增一筆支出了**
   >
   > **請你在 iPhone 上試（約 2 分鐘）**
   > 1. 打開 app → 應該看到空白的清單
   > 2. 點右上角「＋」→ 輸入「午餐 120」→ 點「儲存」
   > 3. 清單上應該出現「午餐 120」
   > 4. 把 app 完全關掉再打開，剛剛那筆還在嗎？
   >
   > **這份改動的檢查狀況**
   > 沒有人類讀過這份程式碼。審查的是 AI……

5. 試完回「**OK**」，或直接描述哪裡不對（例如「按了儲存沒反應」）。

第一次要裝到 iPhone 時，有三件事要你自己動手：在 Xcode 登入 Apple 帳號、在 iPhone 打開開發者模式、信任這個開發者。AI 會一步一步帶你。

## 改壞了怎麼辦

說「**回到上一版**」。AI 會列出之前每個能用的版本讓你選，舊的版本都會留著，不會刪掉。

如果中間改過資料的存法，退回去可能會少掉之後新增的資料；遇到這種情況，AI 會先告訴你會少掉什麼，等你同意才退。

## 目前做不到的事

這個版本只做**資料只存在這支手機上的 app**。下面這些功能出錯時，你試用時看不出來，AI 的檢查也擋不住，所以暫時不做：

- 帳號登入、付款與訂閱
- 需要連網的功能：跟伺服器或其他服務連線、在多台裝置之間同步、雲端備份
- 敏感個資：健康、財務、精確位置、聯絡人，或把照片上傳到任何地方
- 上架 App Store、TestFlight 測試

你提出這類需求時，AI 會直接告訴你做不到的原因，並給你兩條路：先做只存在手機裡的版本，或把 app 整理好交給工程師接手。

## 要知道的事

- **沒有人類讀過 AI 寫的程式碼**。每張試用卡都會寫明這件事，以及這次有哪些 AI 審查過。app 如果要給很多人用，建議請工程師看過再上架。
- **用免費 Apple 帳號裝到 iPhone 的 app，大約 7 天後會打不開**，跟 AI 說一聲它就會重新幫你裝。免費帳號同時最多裝 3 個自己做的 app。
- 想更仔細檢查，可以讓另一家公司的 AI（Codex）也看一次：第一次使用時 AI 會問你，需要 ChatGPT 帳號。這個選項只有「主要用 Claude、也裝了 Codex」時才有。

## 解除安裝

把這句話貼給 Claude 或 Codex：「請在 ~/Developer/ios-dev-skill 執行 ./install.sh --vibe --uninstall，並告訴我移除了什麼。」
