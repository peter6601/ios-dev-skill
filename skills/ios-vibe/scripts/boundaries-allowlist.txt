# vibe 專案的相依白名單（check-boundaries.py 讀這份）
#
# v1 預設不准加任何第三方套件，所以這份清單一開始是空的。
#
# 怎麼加：一行一個套件名稱（不分大小寫），就是套件網址的最後一段、
# CocoaPods 的 pod 名稱、或 Carthage 的 repo 名稱。例如：
#
#     swift-collections
#
# 加之前先想清楚：套件會不會連網、會不會自己存資料。
# 這兩件事一旦發生，試用卡與執行期攔截都擋不住，等於繞過 v1 的範圍限制。
# 加進來之後，記得在專案的 CLAUDE.md／AGENTS.md 寫一句為什麼。
